# UI 스킨 + 터치 영역 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 세 화면(LevelSelect/Editor/Game)에 밝은 종이 톤 코드 테마 + Kenney 아이콘을 입히고, 도구를 오른쪽 세로 패널로 옮기며 보드를 화면에 자동 맞춤한다.

**Architecture:** `AppTheme`(정적 캐시 Theme) 를 각 화면 루트가 적용. Editor·Game 이 복제하던 보드를 `Board` 컴포넌트로 추출하고 `fit_to(area)` 로 칸 크기를 자동 계산. 화면 스크립트는 `%UniqueName` 으로 노드를 잡고, 테스트는 노드 경로 대신 스크립트 프로퍼티(`ed.grid`, `ed.board`)를 쓴다.

**Tech Stack:** Godot 4.7 GDScript, 자체 헤드리스 러너 `tools/test.sh`.

**Spec:** `docs/superpowers/specs/2026-09-19-ui-skin-design.md`

## Global Constraints

- 팔레트: BG `#F4F1E8`, PANEL `#FBFAF5`, INK `#2B2B2B`, ACCENT `#2A7F7A`, MARK `#B5483B`
- 도구 패널·헤더·클리어 패널 버튼 최소 64×64, 패널 VBox separation 8
- 버튼 모서리 10px, 테두리 없음, 기본 글꼴 크기 18
- 오토로드 금지. 화면 전환은 기존 「자식으로 띄우고 back_requested」 패턴 유지, 각 화면 최상위 콘텐츠 노드 이름은 `MarginContainer` 유지(LevelSelect/Editor 가 `$MarginContainer.visible` 로 숨김)
- 아이콘: Kenney Game Icons(CC0) `PNG/White/2x` 에서 복사, 테마 icon color 로 틴트
- 검증 게이트: 매 태스크 끝 `tools/test.sh` exit 0

---

### Task 1: AppTheme + 아이콘 자산 + 그리기 색 통일

**Files:**
- Create: `scripts/ui/AppTheme.gd`, `assets/icons/{back,fill,mark,retry,play,save,plus,trash}.png`, `assets/icons/LICENSE.txt`
- Modify: `scripts/NGrid.gd` (_draw_cell 색), `scripts/HintBar.gd` (배경·글자 색, `slot_px_for`, `depth()` 공개)
- Test: `tests/test_app_theme.gd`

**Interfaces:**
- Produces: `AppTheme.get_theme() -> Theme`, 상수 `AppTheme.BG/PANEL/INK/ACCENT/MARK`, `AppTheme.BUTTON_MIN := 64.0`; theme type variation `AccentButton`, `SegmentButton`, `CardButton`(base `Button`); `HintBar.slot_px_for(cell: float) -> float`(static), `HintBar.depth() -> int`

- [ ] **Step 1: 아이콘 복사** — scratchpad 의 `kenney_game-icons.zip` 을 풀어 `PNG/White/2x/` 에서 arrowLeft→back, stop→fill, cross→mark, return→retry, right→play, save→save, plus→plus, trashcan→trash 로 `assets/icons/` 에 복사. `license.txt` 를 `assets/icons/LICENSE.txt` 로 복사하고 첫 줄에 출처 `https://kenney.nl/assets/game-icons` 추가.

- [ ] **Step 2: 실패 테스트** `tests/test_app_theme.gd`

```gdscript
extends "res://tests/test_case.gd"

func test_theme_is_cached() -> void:
	assert_true(AppTheme.get_theme() == AppTheme.get_theme())

func test_variations_exist() -> void:
	var t := AppTheme.get_theme()
	for v in ["AccentButton", "SegmentButton", "CardButton"]:
		assert_eq(t.get_type_variation_base(v), &"Button", v)

func test_label_uses_ink() -> void:
	assert_eq(AppTheme.get_theme().get_color("font_color", "Label"), AppTheme.INK)

func test_icons_load() -> void:
	for n in ["back", "fill", "mark", "retry", "play", "save", "plus", "trash"]:
		assert_true(load("res://assets/icons/%s.png" % n) is Texture2D, n)

func test_slot_px_for_matches_instance() -> void:
	var b := HintBar.new()
	b.cell_px = 50.0
	assert_eq(HintBar.slot_px_for(50.0), b.slot_px())
	b.free()
```

- [ ] **Step 3: 실패 확인** — `tools/test.sh` → Parse Error(AppTheme 없음)

- [ ] **Step 4: 구현** `scripts/ui/AppTheme.gd`

```gdscript
class_name AppTheme extends RefCounted
## 앱 공용 테마 (밝은 종이 톤). 화면 루트가 _ready 에서 theme = AppTheme.get_theme()

const BG := Color("#F4F1E8")
const PANEL := Color("#FBFAF5")
const INK := Color("#2B2B2B")
const ACCENT := Color("#2A7F7A")
const MARK := Color("#B5483B")
const BUTTON_BASE := Color("#E6E1D3")
const BUTTON_MIN := 64.0

static var _theme: Theme

static func get_theme() -> Theme:
	if _theme == null:
		_theme = _build()
	return _theme

static func _box(color: Color, radius := 10, pad := 12.0) -> StyleBoxFlat:
	var sb := StyleBoxFlat.new()
	sb.bg_color = color
	sb.set_corner_radius_all(radius)
	sb.set_content_margin_all(pad)
	return sb

## normal/hover/pressed/hover_pressed/disabled 스타일과 글자·아이콘 색을 한 번에
static func _button_type(t: Theme, type: String, normal: Color, pressed: Color, fg: Color, pressed_fg: Color) -> void:
	t.set_stylebox("normal", type, _box(normal))
	t.set_stylebox("hover", type, _box(normal.darkened(0.06)))
	t.set_stylebox("pressed", type, _box(pressed))
	t.set_stylebox("hover_pressed", type, _box(pressed.darkened(0.06)))
	t.set_stylebox("disabled", type, _box(normal.lightened(0.4)))
	t.set_stylebox("focus", type, StyleBoxEmpty.new())
	for c in ["font_color", "font_hover_color", "font_focus_color", "icon_normal_color", "icon_hover_color", "icon_focus_color"]:
		t.set_color(c, type, fg)
	for c in ["font_pressed_color", "font_hover_pressed_color", "icon_pressed_color", "icon_hover_pressed_color"]:
		t.set_color(c, type, pressed_fg)

static func _build() -> Theme:
	var t := Theme.new()
	t.default_font_size = 18

	_button_type(t, "Button", BUTTON_BASE, BUTTON_BASE.darkened(0.12), INK, INK)
	t.set_constant("icon_max_width", "Button", 28)
	t.set_constant("h_separation", "Button", 10)

	t.set_type_variation("AccentButton", "Button")
	_button_type(t, "AccentButton", ACCENT, ACCENT.darkened(0.15), Color.WHITE, Color.WHITE)

	t.set_type_variation("SegmentButton", "Button")  # 토글: 눌린 쪽이 ACCENT
	_button_type(t, "SegmentButton", BUTTON_BASE, ACCENT, INK, Color.WHITE)

	t.set_type_variation("CardButton", "Button")  # 목록 행
	_button_type(t, "CardButton", PANEL, BUTTON_BASE, INK, INK)

	t.set_color("font_color", "Label", INK)

	var edit := _box(Color.WHITE, 8, 10.0)
	var edit_focus := _box(Color.WHITE, 8, 10.0)
	edit_focus.set_border_width_all(2)
	edit_focus.border_color = ACCENT
	t.set_stylebox("normal", "LineEdit", edit)
	t.set_stylebox("focus", "LineEdit", edit_focus)
	t.set_color("font_color", "LineEdit", INK)
	t.set_color("caret_color", "LineEdit", INK)
	t.set_color("font_placeholder_color", "LineEdit", Color(INK, 0.45))

	t.set_stylebox("panel", "PanelContainer", _box(PANEL, 14, 12.0))
	t.set_stylebox("panel", "PopupMenu", _box(PANEL, 8, 8.0))
	t.set_stylebox("hover", "PopupMenu", _box(BUTTON_BASE, 6, 4.0))
	t.set_color("font_color", "PopupMenu", INK)
	t.set_color("font_hover_color", "PopupMenu", INK)
	return t
```

`scripts/HintBar.gd` 변경:

```gdscript
## 숫자 한 개가 차지하는 칸 폭(ROW) / 높이(COL)
func slot_px() -> float:
	return slot_px_for(cell_px)

static func slot_px_for(cell: float) -> float:
	return maxf(cell * 0.6, MIN_SLOT_PX)
```
`_depth()` → `depth()` 로 이름 변경(호출부 2곳 포함). `_draw` 배경 `Color(0.94, 0.94, 0.92)` → `AppTheme.PANEL`, 글자 `Color(0.15, 0.15, 0.15)` → `AppTheme.INK`.

`scripts/NGrid.gd` `_draw_cell`: 채움 `Color.BLACK` → `AppTheme.INK`, X 선 `Color(0.8, 0.2, 0.2)` → `AppTheme.MARK`.

- [ ] **Step 5: 통과 확인** — `tools/test.sh` exit 0
- [ ] **Step 6: 커밋** — `feat(ui): AppTheme 코드 테마 + Kenney 아이콘 + 그리기 색 통일`

---

### Task 2: Board 컴포넌트 + 자동 맞춤

**Files:**
- Create: `scripts/ui/Board.gd`, `scenes/ui/Board.tscn`
- Test: `tests/test_board.gd`

**Interfaces:**
- Consumes: `HintBar.slot_px_for`, `HintBar.depth()`
- Produces: `class_name Board extends GridContainer` — `grid: NGrid`, `row_hints: HintBar`, `col_hints: HintBar`, `set_hints(rows: Array, cols: Array)`, `set_min_depth(row_depth: int, col_depth: int)`, `fit_to(area: Vector2) -> float`, static `calc_cell_px(area: Vector2, sz: Vector2i, row_depth: int, col_depth: int, sep: float) -> float`, 상수 `MIN_CELL := 24.0`, `MAX_CELL := 72.0`

- [ ] **Step 1: 실패 테스트** `tests/test_board.gd`

```gdscript
extends "res://tests/test_case.gd"

# 1152×648 기본 / 1556×648 Pixel 비율에서 여백·패널·헤더를 뺀 보드 영역
const AREAS := [Vector2(884, 544), Vector2(1288, 544)]
const SEP := 4.0

func _fits(area: Vector2, sz: Vector2i, d: int, cell: float) -> bool:
	var slot := HintBar.slot_px_for(cell)
	return sz.x * cell + d * slot + SEP <= area.x and sz.y * cell + d * slot + SEP <= area.y

func test_calc_cell_fits_and_is_clamped() -> void:
	for sz in [Vector2i(5, 5), Vector2i(10, 10), Vector2i(15, 15)]:
		var d := ceili(sz.x / 2.0)
		for area in AREAS:
			var cell := Board.calc_cell_px(area, sz, d, d, SEP)
			assert_true(cell >= Board.MIN_CELL and cell <= Board.MAX_CELL, "range %s %s" % [sz, cell])
			assert_true(_fits(area, sz, d, cell), "fits %s %s %s" % [sz, area, cell])

func test_calc_cell_is_largest_fit() -> void:
	var sz := Vector2i(10, 10)
	var cell := Board.calc_cell_px(AREAS[0], sz, 5, 5, SEP)
	assert_true(cell == Board.MAX_CELL or not _fits(AREAS[0], sz, 5, cell + 1.0))

func test_tiny_area_falls_back_to_min() -> void:
	assert_eq(Board.calc_cell_px(Vector2.ZERO, Vector2i(10, 10), 5, 5, SEP), Board.MIN_CELL)

func test_fit_to_applies_cell_everywhere() -> void:
	var b: Board = add_node(load("res://scenes/ui/Board.tscn").instantiate())
	b.set_min_depth(5, 5)
	b.set_hints([[0]], [[0]])
	var cell := b.fit_to(Vector2(600, 400))
	assert_eq(b.grid.cell_px, cell, "grid")
	assert_eq(b.row_hints.cell_px, cell, "rows")
	assert_eq(b.col_hints.cell_px, cell, "cols")
	assert_eq(b.grid.custom_minimum_size, Vector2(b.grid.grid_size) * cell, "grid size")
```

- [ ] **Step 2: 실패 확인** — Parse Error(Board 없음)

- [ ] **Step 3: 구현** `scripts/ui/Board.gd`

```gdscript
class_name Board extends GridContainer
## 힌트바 2개 + NGrid 묶음. [Corner][ColHints] / [RowHints][NGrid]
## fit_to(area) 로 area 안에 들어가는 최대 칸 크기를 적용한다.

const MIN_CELL := 24.0
const MAX_CELL := 72.0

@onready var grid: NGrid = $NGrid
@onready var row_hints: HintBar = $RowHints
@onready var col_hints: HintBar = $ColHints

func set_hints(rows: Array, cols: Array) -> void:
	row_hints.set_hints(rows)
	col_hints.set_hints(cols)

func set_min_depth(row_depth: int, col_depth: int) -> void:
	row_hints.min_depth = row_depth
	col_hints.min_depth = col_depth

func fit_to(area: Vector2) -> float:
	var sep := float(get_theme_constant("h_separation"))
	var cell := calc_cell_px(area, grid.grid_size, row_hints.depth(), col_hints.depth(), sep)
	grid.cell_px = cell
	grid.custom_minimum_size = Vector2(grid.grid_size) * cell
	grid.size = grid.custom_minimum_size
	grid.queue_redraw()
	row_hints.cell_px = cell
	col_hints.cell_px = cell
	set_hints(row_hints.hints, col_hints.hints)  # 최소 크기 재계산
	return cell

static func calc_cell_px(area: Vector2, sz: Vector2i, row_depth: int, col_depth: int, sep: float) -> float:
	var cell := MAX_CELL
	while cell > MIN_CELL:
		var slot := HintBar.slot_px_for(cell)
		if sz.x * cell + row_depth * slot + sep <= area.x and sz.y * cell + col_depth * slot + sep <= area.y:
			return cell
		cell -= 1.0
	return MIN_CELL
```

`scenes/ui/Board.tscn`

```
[gd_scene load_steps=4 format=3]

[ext_resource type="Script" path="res://scripts/ui/Board.gd" id="1"]
[ext_resource type="Script" path="res://scripts/NGrid.gd" id="2"]
[ext_resource type="Script" path="res://scripts/HintBar.gd" id="3"]

[node name="Board" type="GridContainer"]
theme_override_constants/h_separation = 4
theme_override_constants/v_separation = 4
columns = 2
script = ExtResource("1")

[node name="Corner" type="Control" parent="."]
layout_mode = 2

[node name="ColHints" type="Control" parent="."]
layout_mode = 2
script = ExtResource("3")
axis = 1

[node name="RowHints" type="Control" parent="."]
layout_mode = 2
script = ExtResource("3")

[node name="NGrid" type="Control" parent="."]
layout_mode = 2
script = ExtResource("2")
```

- [ ] **Step 4: 통과 확인** — `tools/test.sh` exit 0
- [ ] **Step 5: 커밋** — `feat(ui): Board 컴포넌트 추출 + 화면 맞춤 칸 크기 계산`

---

### Task 3: Game 화면 재배치 (펜 세그먼트·클리어 패널)

**Files:**
- Modify: `scenes/Game.tscn`(전면 재작성), `scripts/GameScene.gd`(전면 재작성)
- Test: `tests/test_game.gd`(경로→프로퍼티, 펜 테스트 교체, 버튼 크기 테스트 추가)

**Interfaces:**
- Consumes: `Board`, `AppTheme`
- Produces: GameScene 프로퍼티 `board`, `grid`, `title_label`, `fill_btn`, `mark_btn`, `clear_panel`, `level`; 메서드 `_on_retry_pressed()`, `_on_back_pressed()`; 시그널 `back_requested`

- [ ] **Step 1: 테스트 수정** `tests/test_game.gd`
  - `_grid(g)` → `return g.grid`; `BOARD + "RowHints"` → `g.board.row_hints`, `"ColHints"` → `g.board.col_hints`; TitleLabel → `g.title_label`
  - `test_pen_toggle_switches_primary_to_x` 를 교체:

```gdscript
func test_pen_segment() -> void:
	var g := _game(_level(["#.", ".#"]))
	var grid := _grid(g)
	assert_true(g.fill_btn.button_pressed, "fill default")
	g.mark_btn.button_pressed = true
	assert_true(not g.fill_btn.button_pressed, "exclusive")
	assert_eq(grid.pen, 2, "pen x")
	_click(grid, 0, 0)
	assert_eq(grid.state[0][0], 2, "x mark")
	g.fill_btn.button_pressed = true
	_click(grid, 1, 0)
	assert_eq(grid.state[0][1], 1, "fill again")

func test_touch_targets_min_64() -> void:
	var g := _game(_level(["#"]))
	for b in g.find_children("*", "BaseButton", true, false):
		var s: Vector2 = b.get_combined_minimum_size()
		assert_true(s.x >= AppTheme.BUTTON_MIN and s.y >= AppTheme.BUTTON_MIN, "%s %s" % [b.name, s])

func test_theme_applied() -> void:
	var g := _game(_level(["#"]))
	assert_true(g.theme == AppTheme.get_theme())
```

- [ ] **Step 2: 실패 확인** — `tools/test.sh` 실패(fill_btn 없음 등)

- [ ] **Step 3: 구현** `scripts/GameScene.gd`

```gdscript
extends Control
## 플레이 화면. 여는 쪽이 level 을 넣고 트리에 붙인 뒤, back_requested 를 받아 닫는다.

signal back_requested

@onready var board: Board = %Board
@onready var grid: NGrid = board.grid
@onready var board_area: Control = %BoardArea
@onready var title_label: Label = %TitleLabel
@onready var fill_btn: Button = %FillBtn
@onready var mark_btn: Button = %MarkBtn
@onready var clear_panel: Control = $ClearPanel
@onready var clear_label: Label = %ClearLabel

var level: LevelData

func _ready() -> void:
	theme = AppTheme.get_theme()
	grid.completed.connect(_on_completed)
	fill_btn.toggled.connect(func(on: bool): if on: grid.pen = 1)
	mark_btn.toggled.connect(func(on: bool): if on: grid.pen = 2)
	board_area.resized.connect(_fit_board)
	clear_panel.visible = false
	if level == null:
		title_label.text = "레벨 없음"
		return
	title_label.text = level.title
	grid.mode = NGrid.Mode.PLAY
	grid.load_level(level)
	board.set_hints(level.row_hints, level.col_hints)
	fill_btn.button_pressed = true
	_fit_board()

func _fit_board() -> void:
	board.fit_to(board_area.size)

func _on_completed() -> void:
	grid.interactive = false
	clear_label.text = "클리어!\n%s" % level.title
	clear_panel.visible = true

func _on_retry_pressed() -> void:
	grid.reset_state()
	grid.interactive = true
	clear_panel.visible = false

func _on_back_pressed() -> void:
	back_requested.emit()
```

`scenes/Game.tscn`

```
[gd_scene load_steps=8 format=3]

[ext_resource type="Script" path="res://scripts/GameScene.gd" id="1"]
[ext_resource type="PackedScene" path="res://scenes/ui/Board.tscn" id="2"]
[ext_resource type="Texture2D" path="res://assets/icons/back.png" id="3"]
[ext_resource type="Texture2D" path="res://assets/icons/fill.png" id="4"]
[ext_resource type="Texture2D" path="res://assets/icons/mark.png" id="5"]
[ext_resource type="Texture2D" path="res://assets/icons/retry.png" id="6"]

[sub_resource type="ButtonGroup" id="pen_group"]

[node name="Game" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
script = ExtResource("1")

[node name="Background" type="ColorRect" parent="."]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
color = Color(0.956863, 0.945098, 0.909804, 1)

[node name="MarginContainer" type="MarginContainer" parent="."]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
theme_override_constants/margin_left = 16
theme_override_constants/margin_top = 16
theme_override_constants/margin_right = 16
theme_override_constants/margin_bottom = 16

[node name="HBox" type="HBoxContainer" parent="MarginContainer"]
layout_mode = 2
theme_override_constants/separation = 16

[node name="Main" type="VBoxContainer" parent="MarginContainer/HBox"]
layout_mode = 2
size_flags_horizontal = 3
theme_override_constants/separation = 8

[node name="Header" type="HBoxContainer" parent="MarginContainer/HBox/Main"]
layout_mode = 2
theme_override_constants/separation = 12

[node name="BackBtn" type="Button" parent="MarginContainer/HBox/Main/Header"]
unique_name_in_owner = true
custom_minimum_size = Vector2(64, 64)
layout_mode = 2
icon = ExtResource("3")
icon_alignment = 1

[node name="TitleLabel" type="Label" parent="MarginContainer/HBox/Main/Header"]
unique_name_in_owner = true
layout_mode = 2
theme_override_font_sizes/font_size = 26

[node name="BoardArea" type="CenterContainer" parent="MarginContainer/HBox/Main"]
unique_name_in_owner = true
layout_mode = 2
size_flags_vertical = 3

[node name="Board" parent="MarginContainer/HBox/Main/BoardArea" instance=ExtResource("2")]
unique_name_in_owner = true
layout_mode = 2

[node name="SidePanel" type="PanelContainer" parent="MarginContainer/HBox"]
custom_minimum_size = Vector2(220, 0)
layout_mode = 2

[node name="Tools" type="VBoxContainer" parent="MarginContainer/HBox/SidePanel"]
unique_name_in_owner = true
layout_mode = 2
theme_override_constants/separation = 8

[node name="FillBtn" type="Button" parent="MarginContainer/HBox/SidePanel/Tools"]
unique_name_in_owner = true
custom_minimum_size = Vector2(64, 64)
layout_mode = 2
theme_type_variation = &"SegmentButton"
toggle_mode = true
button_group = SubResource("pen_group")
text = "채우기"
icon = ExtResource("4")

[node name="MarkBtn" type="Button" parent="MarginContainer/HBox/SidePanel/Tools"]
unique_name_in_owner = true
custom_minimum_size = Vector2(64, 64)
layout_mode = 2
theme_type_variation = &"SegmentButton"
toggle_mode = true
button_group = SubResource("pen_group")
text = "X 표시"
icon = ExtResource("5")

[node name="Spacer" type="Control" parent="MarginContainer/HBox/SidePanel/Tools"]
layout_mode = 2
size_flags_vertical = 3

[node name="RetryBtn" type="Button" parent="MarginContainer/HBox/SidePanel/Tools"]
custom_minimum_size = Vector2(64, 64)
layout_mode = 2
text = "처음부터"
icon = ExtResource("6")

[node name="ClearPanel" type="ColorRect" parent="."]
visible = false
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
color = Color(0, 0, 0, 0.45)

[node name="Center" type="CenterContainer" parent="ClearPanel"]
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0

[node name="Panel" type="PanelContainer" parent="ClearPanel/Center"]
custom_minimum_size = Vector2(320, 0)
layout_mode = 2

[node name="VBox" type="VBoxContainer" parent="ClearPanel/Center/Panel"]
layout_mode = 2
theme_override_constants/separation = 12

[node name="ClearLabel" type="Label" parent="ClearPanel/Center/Panel/VBox"]
unique_name_in_owner = true
layout_mode = 2
theme_override_colors/font_color = Color(0.164706, 0.498039, 0.478431, 1)
theme_override_font_sizes/font_size = 30
horizontal_alignment = 1

[node name="RetryBtn" type="Button" parent="ClearPanel/Center/Panel/VBox"]
custom_minimum_size = Vector2(64, 64)
layout_mode = 2
text = "다시 하기"
icon = ExtResource("6")

[node name="BackBtn" type="Button" parent="ClearPanel/Center/Panel/VBox"]
custom_minimum_size = Vector2(64, 64)
layout_mode = 2
theme_type_variation = &"AccentButton"
text = "돌아가기"
icon = ExtResource("3")

[connection signal="pressed" from="MarginContainer/HBox/Main/Header/BackBtn" to="." method="_on_back_pressed"]
[connection signal="pressed" from="MarginContainer/HBox/SidePanel/Tools/RetryBtn" to="." method="_on_retry_pressed"]
[connection signal="pressed" from="ClearPanel/Center/Panel/VBox/RetryBtn" to="." method="_on_retry_pressed"]
[connection signal="pressed" from="ClearPanel/Center/Panel/VBox/BackBtn" to="." method="_on_back_pressed"]
```

- [ ] **Step 4: 통과 확인** — `tools/test.sh` exit 0
- [ ] **Step 5: 커밋** — `feat(ui): Game 화면 세로 도구 패널 + 펜 세그먼트 + 테마`

---

### Task 4: Editor 화면 재배치

**Files:**
- Modify: `scenes/Editor.tscn`(전면 재작성), `scripts/EditorScene.gd`, `scripts/NGrid.gd`(`cell_px_for` 제거)
- Test: `tests/test_scenes.gd`, `tests/test_level_select.gd`(BackBtn 경로)

**Interfaces:**
- Consumes: `Board`, `AppTheme`
- Produces: EditorScene 프로퍼티 `board`, `grid`, `title_edit`, `status_label`, `back_btn`, `size_option`, `storage`, `show_back`, `current_id`; 메서드 `_on_save_pressed/_on_play_pressed/_on_clear_pressed/_on_back_pressed/_on_size_option_item_selected(index)`. **`_on_print_pressed` 제거**

- [ ] **Step 1: 테스트 수정**
  - `tests/test_scenes.gd`: `BOARD + "NGrid"` → `ed.grid`, `"RowHints"` → `ed.board.row_hints`, `"ColHints"` → `ed.board.col_hints`, `TopBar/TitleEdit` → `ed.title_edit`, `VBox/StatusLabel` → `ed.status_label`, `TopBar/BackBtn` → `ed.back_btn`, 게임 쪽 `game.get_node(BOARD + "NGrid")` → `game.grid`. `BOARD` 상수 삭제. 추가:

```gdscript
func test_editor_touch_targets_and_theme() -> void:
	var ed := _editor()
	assert_true(ed.theme == AppTheme.get_theme(), "theme")
	for b in ed.find_children("*", "BaseButton", true, false):
		var s: Vector2 = b.get_combined_minimum_size()
		assert_true(s.x >= AppTheme.BUTTON_MIN and s.y >= AppTheme.BUTTON_MIN, "%s %s" % [b.name, s])
```
  - `tests/test_level_select.gd`: `ed.get_node("MarginContainer/VBox/TopBar/BackBtn")` → `ed.back_btn`

- [ ] **Step 2: 실패 확인**

- [ ] **Step 3: 구현** `scripts/EditorScene.gd` 상단·`_ready`·힌트/크기 부분 교체 (저장·플레이·뒤로 로직은 유지):

```gdscript
extends Control

signal back_requested  # LevelSelect 에서 열었을 때 돌아가기

const GameScene := preload("res://scenes/Game.tscn")
const SIZES := [Vector2i(5, 5), Vector2i(10, 10), Vector2i(15, 15)]

@onready var board: Board = %Board
@onready var grid: NGrid = board.grid
@onready var board_area: Control = %BoardArea
@onready var size_option: OptionButton = %SizeOption
@onready var title_edit: LineEdit = %TitleEdit
@onready var status_label: Label = %StatusLabel
@onready var back_btn: Button = %BackBtn

var storage := LevelStorage.new()
var show_back := false  # 여는 쪽이 트리에 붙이기 전에 설정
var current_id := ""  # 저장된 레벨을 편집 중이면 그 id — 재저장 시 덮어쓴다

func _ready() -> void:
	theme = AppTheme.get_theme()
	back_btn.visible = show_back
	grid.solution_changed.connect(_refresh_hints)
	board_area.resized.connect(_fit_board)
	_setup_size_options()
	_refresh_hints()
	_fit_board()

func _setup_size_options() -> void:
	size_option.clear()
	for sz in SIZES:
		size_option.add_item("%d × %d" % [sz.x, sz.y])
	size_option.selected = 1  # 10×10 기본

func _refresh_hints() -> void:
	var level := grid.get_level_data()
	# 한 줄 힌트 개수의 최대치 = ceil(길이/2) — 미리 예약해 그리는 중 격자가 밀리지 않게
	board.set_min_depth(ceili(grid.grid_size.x / 2.0), ceili(grid.grid_size.y / 2.0))
	board.set_hints(level.row_hints, level.col_hints)

func _fit_board() -> void:
	board.fit_to(board_area.size)

func _on_size_option_item_selected(index: int) -> void:
	grid.grid_size = SIZES[index]
	grid._init_arrays()
	_refresh_hints()
	_fit_board()
	_start_new_level()
```
`_on_print_pressed` 삭제. `_on_play_pressed`/`_close_game` 의 `$MarginContainer` 사용은 유지.

`scripts/NGrid.gd`: `cell_px_for` 함수와 주석 삭제.

`scenes/Editor.tscn` — Game.tscn 과 같은 틀(Background/MarginContainer/HBox/Main[Header,BoardArea/Board]/SidePanel/Tools):
  - Header: BackBtn(64×64, back 아이콘, `visible = false`, unique), TitleLabel("레벨 만들기", 26)
  - Tools: SizeOption(OptionButton, unique, min 64×64), TitleEdit(LineEdit, unique, min 0×64, placeholder "레벨 제목", max_length 40), SaveBtn(AccentButton, save 아이콘, "저장", min 64×64), PlayBtn(play 아이콘, "플레이"), ClearBtn(trash 아이콘, "초기화"), Spacer(expand), StatusLabel(unique, autowrap_mode=3, font 14)
  - 연결: BackBtn→_on_back_pressed, SaveBtn→_on_save_pressed, PlayBtn→_on_play_pressed, ClearBtn→_on_clear_pressed, SizeOption.item_selected→_on_size_option_item_selected

- [ ] **Step 4: 통과 확인** — `tools/test.sh` exit 0
- [ ] **Step 5: 커밋** — `feat(ui): Editor 화면 세로 도구 패널 + 테마, 디버그 버튼 제거`

---

### Task 5: LevelSelect 테마 + 캡처 도구 창 크기 + 시각·실기기 확인

**Files:**
- Modify: `scenes/LevelSelect.tscn`, `scripts/LevelSelectScene.gd`, `tools/screenshot.gd`
- Test: `tests/test_level_select.gd`

- [ ] **Step 1: 테스트 추가** `tests/test_level_select.gd`

```gdscript
func test_theme_and_card_rows() -> void:
	var st := _storage()
	_save(st, "1000_0001", "a")
	var s := _select(st)
	assert_true(s.theme == AppTheme.get_theme(), "theme")
	var row: Button = _rows(s)[0]
	assert_eq(row.theme_type_variation, &"CardButton", "card")
	assert_true(row.get_combined_minimum_size().y >= AppTheme.BUTTON_MIN, "row height")
	var new_btn: Button = s.get_node("MarginContainer/VBox/TopBar/NewBtn")
	assert_true(new_btn.get_combined_minimum_size().y >= AppTheme.BUTTON_MIN, "new btn")
```

- [ ] **Step 2: 실패 확인**
- [ ] **Step 3: 구현**
  - `LevelSelectScene.gd` `_ready` 첫 줄 `theme = AppTheme.get_theme()`. `refresh()` 행 버튼: `btn.theme_type_variation = &"CardButton"`, `btn.custom_minimum_size = Vector2(0, AppTheme.BUTTON_MIN)`
  - `LevelSelect.tscn`: 루트 첫 자식 Background(ColorRect, BG 색, 전체 앵커) 추가, 여백 24, Title 폰트 30, NewBtn `theme_type_variation = &"AccentButton"`, plus 아이콘, text "새 레벨", min 64×64
  - `tools/screenshot.gd`: 인자 중 `^\d+x\d+$` 는 창 크기로 `DisplayServer.window_set_size(Vector2i(w, h))` 후 프레임 대기, `solve` 는 플래그로 파싱(위치 무관)
- [ ] **Step 4: 통과 확인** — `tools/test.sh` exit 0
- [ ] **Step 5: 시각 확인** — 세 화면 × `1152x648`, `1556x648` 캡처(Game 은 `solve` 포함 1장 추가) 후 이미지 확인: 버튼 64px·아이콘 틴트·보드 가운데·패널 겹침 없음
- [ ] **Step 6: 실기기 확인** — `adb -s 16091JECB01753` 로 빌드 설치, 전체 흐름 캡처, 기기 설정 원복
- [ ] **Step 7: 문서** — 로컬 CLAUDE.md 구조 절(Board/AppTheme/assets), 위키 「UI 스킨」 완료 표기
- [ ] **Step 8: 커밋** — `feat(ui): LevelSelect 테마 + 캡처 도구 창 크기 옵션`
